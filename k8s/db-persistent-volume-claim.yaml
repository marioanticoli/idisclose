apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: idisclose-postgres-data-persisent-volume-claim
spec:
  volumeName: idisclose-postgres-data-persisent-volume
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
